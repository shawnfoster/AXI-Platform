# AXI Engine Registry

`Runtime/EngineRegistry` provides the governed runtime registry for AXI
engines.

Scope for `M15`:

- engine registration and lookup
- engine lifecycle operations
- validation and dependency checks
- capability exposure and event publication
