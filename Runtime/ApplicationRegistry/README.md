# AXI Application Registry

`Runtime/ApplicationRegistry` provides the governed runtime registry for
AXI applications.

Scope for `M14`:

- application registration and lookup
- application lifecycle operations
- validation and dependency checks
- capability exposure and event publication
