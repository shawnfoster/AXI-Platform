# CODEX_HANDOFF.md — Executive Operational Briefing

## Purpose

This document is the authoritative operational briefing for future
Codex sessions.

It complements, but never replaces, repository governance.

Repository evidence remains authoritative.

Conversation history is not authoritative.

---

# Repository Status

- Current Active Milestone: None (`M23` Complete)
- Current Repository Version: `AXI-Platform-v2.1`
- Current Constitutional Baseline:
  `ADR-0006` through `ADR-0024`, `AXI-SCH-006` through `AXI-SCH-038`,
  `PUB-001` through `PUB-021`, and `DGM-001` through `DGM-010`
- Current Active CTG: `CTG-001` (`Satisfied`)
- Current Runtime Boundary: `M18 Runtime API`
- Current Repository State:
  `M22` governance is complete, `CTG-001` is satisfied, Post-`M22`
  Executive Transition Validation is complete and recorded in
  `PUB-021`, `M23` is complete, `M24` remains the next planned
  constitutional milestone, and no runtime implementation is
  authorized beyond `M18`
- Current Branch: `main`
- Last Approved ADR: `ADR-0024`
- Last Approved Publication: `PUB-021`
- Last Completed Work Item: `M23`
- Last Commit:
  verify live repository state during startup with
  `git log -1 --oneline`

---

# Startup Requirements

Every Codex session shall:

1. Execute repository startup.
2. Read required governance.
3. Verify roadmap.
4. Verify dependency matrix.
5. Verify work queue.
6. Verify active CTGs.
7. Verify repository state.
8. Verify publication numbering.
9. Verify schema consistency.
10. Verify repository evidence before making changes.

---

# Executive Objectives

Primary Objective

- Preserve the completed Post-`M22` Executive Transition Validation
  record, the completed `M23` baseline, and synchronized `CTG-001`
  evidence until a later governed objective is explicitly assigned.

Secondary Objective

- Preserve synchronized constitutional status surfaces across the
  roadmap, CTGs, publication family, prompt-operations baseline,
  schema registry, and handoff state.

Explicitly Unauthorized

- Runtime expansion
- Automatic `M24` start
- Reopening completed Post-`M22` validation without new repository
  evidence
- Operational implementation
- Any work outside repository authority

---

# Repository Philosophy

- Evidence before documentation.
- Repository evidence overrides conversation.
- Constitutional governance precedes implementation.
- Decision Intelligence derives from governed evidence.
- Transition authority derives from repository evidence, not from
  conversational completion claims.

---

# Current Architectural State

Governance Layers

- Repository authority at the root and under `Governance/`
- AI startup and AI governance under `.ai/`
- Publication, schema, diagram, and transition-gate governance as
  first-class constitutional artifacts

Publication Families

- Constitutional Core
- Architecture and Governance Core
- Operational Guidance
- Prompt Operations
- Applied Practice
- Visual Governance

The Constitutional Core now includes the first Architecture Core
Operating System publication.

Schemas

- Decision and platform schemas are published.
- Repository stewardship, publication, diagram, dashboard, widget,
  visualization, Organization, Knowledge, Information Governance
  Profile, Person, Role, Expertise, Policy, Timeline, Resource, and
  Dependency schemas are published.
- Canonical `Knowledge` objects now inherit governed information
  metadata through `AXI-SCH-031` and the updated `AXI-SCH-030`.
- Placeholder schema family `AXI-SCH-001` through `AXI-SCH-005`
  remains unpublished and non-authoritative.

Standards

- Documentation visualization and documentation quality standards are
  published.
- Multiple foundational standards files still exist as placeholders and
  remain non-authoritative.

Transition Gates

- Constitutional Transition Gates are first-class governed artifacts.
- `CTG-001` is published and satisfied.

Runtime Boundary

- Runtime implementation is intentionally limited to the reusable
  foundation through `M18 Runtime API`.

---

# Active Constitutional Transition Gates

`CTG-001`

- Current State: Satisfied
- Activation Criteria:
  all published `M22` acceptance evidence plus synchronized work queue,
  README, roadmap, handoff, publication, ADR, and dependency state
- Repository Evidence:
  `Governance/TransitionGates/CTG-001_M22_Completion_Gate.md`
- Authorized Next Step:
  Historical next step fulfilled by
  `Governance/Publications/AXI_Post_M22_Executive_Transition_Validation_Record.md`;
  do not begin `M24` automatically

---

# Prompt Operations Manual (POM)

Authoritative POM publications:

- `Governance/Publications/PromptOperations/AXI_Prompt_Operations_Manual.md`
- `Governance/Publications/PromptOperations/AXI_Prompt_Operations_Index.md`

Prompt categories:

- Startup
- Architecture
- Governance
- Milestones
- Operational Validation
- Executive
- Emergency

Milestone prompt organization:

- Prompt routes use milestone identifiers such as `POM-M22-001`.
- Current repository evidence publishes milestone coverage for `M8`
  through `M18`, `M21`, `M22`, `M23`, and `M24`.
- No governed milestone prompt routes currently exist for `M1` through
  `M7`, `M19`, or `M20`.

Operational rule:

- POM routes govern prompt references and route-state visibility.
- POM routes do not override repository authority or protected-
  knowledge posture.
- Dependent operational-validation routes remain gated until their
  governing CTGs are satisfied, then become historical only when a
  completion record is published.

---

# Architectural Debt

High

- Foundational standards home remains partially unpublished:
  `AXI_Standards_Index_v1.0.md`, `DIRECTORY_STANDARD.md`,
  `ID_STANDARD.md`, `NAMING.md`, `RELEASE_POLICY.md`, and
  `VERSIONING.md` remain placeholders.

Medium

- Historical milestone numbering drift remains documented rather than
  normalized; current governance starts from the published `M8`
  through `M18`, then `M21` through `M24`.

Low

- GUI governance surface remains intentionally thin and unpublished
  beyond current repository placeholders because no GUI milestone is
  authorized.

---

# Repository Health

Overall repository health is stable at the constitutional layer.

Current risks:

- Placeholder standards continue to leave a known governance gap for
  foundational repository normalization.
- Later milestone work could drift if future sessions treat completed
  `M23` as implicit authorization for `M24`.

Current opportunities:

- Use `PUB-021` to keep future startup and transition review entirely
  repository-driven.
- Use the Prompt Operations baseline to keep future reviews and
  milestone work repository-driven.
- Use `M24` to keep future cross-domain governance aligned to one
  constitutional foundation when later milestones are explicitly
  assigned.

Outstanding governance work:

- Eventual `M24` execution after explicit authorization, with the
  completed `M23` baseline preserved as upstream authority
- Future transition-gate expansion only when repository evidence
  requires it

---

# Executive Recommendations

- Preserve synchronized evidence for completed Post-`M22` validation,
  completed `M23`, and satisfied `CTG-001`.
- Do not auto-open `M24` or later milestone work without explicit
  authorization.
- Use `M24` as the constitutional foundation for future cross-domain
  governance instead of introducing new architectural abstractions.
- Use governed POM identifiers rather than conversational prompt recall
  for future startup, review, and milestone routing.
- Do not authorize runtime or operational implementation beyond the
  current repository boundary.

---

# Success Criteria

The current objective remains complete only when:

- the assigned repository objective is satisfied by repository evidence
- repository consistency is preserved
- status surfaces remain synchronized
- schema and publication numbering remain valid
- any CTG-dependent transition is validated through the governing gate

For the completed Post-`M22` transition and `M23` state, success
remains governed by `CTG-001`, `PUB-021`, the published `M23` work
item, and the synchronized repository status surfaces.

Completion of this handoff does not authorize `M24` or any later
milestone automatically.

---

# Session Exit Checklist

Before ending the session verify:

- Repository consistency
- Schema validation
- Publication numbering
- Register updates
- Roadmap updates
- Dependency updates when required
- Handoff updates
- Commit message prepared
- Repository ready for next session

---

# Constitutional Rule

This handoff is an executive operational briefing.

It summarizes repository state.

It never supersedes repository evidence.

If any statement conflicts with repository evidence, repository
evidence shall prevail.
