# ADR-0012 — Runtime CLI Surface Boundary

## Status

Placeholder

---

## Purpose

Reserve the milestone-specific ADR required by `M17 Runtime CLI`.

This file is a planning artifact only and is not authoritative
architecture.

---

## Repository Evidence

Published repository evidence supporting this placeholder is limited to:

- `Governance/DependencyMatrix.md` naming future runtime CLI milestones
  as downstream consumers of `M16`
- `Governance/ADR/ADR-0011_Pipeline_Runtime_Boundary.md` excluding CLI
  execution surfaces from `M16`
- `Governance/Schemas/AXI-SCH-007_Platform_Object.json` publishing the
  `AXI-CLI` namespace and `CLI` object type
- the absence of `Runtime/CLI/` implementation in the repository

---

## Placeholder Rule

This ADR does not approve command semantics, transport behavior,
serialization rules, or new runtime architecture.

Implementation may not begin until approved ADR content replaces this
placeholder.
