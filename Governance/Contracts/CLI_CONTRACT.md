# AXI Runtime CLI Contract

**Version:** 0.1.0
**Status:** Placeholder
**Authority:** AXI Platform Governance

---

# Purpose

Reserve the milestone-specific contract required by `M17 Runtime CLI`.

This file is a planning artifact only.

---

# Repository Evidence

Phase II planning for the runtime CLI is justified by published
repository evidence:

- `Governance/DependencyMatrix.md` names future runtime CLI milestones
  as downstream consumers of `M16`.
- `Governance/ADR/ADR-0011_Pipeline_Runtime_Boundary.md` excludes CLI
  execution surfaces from `M16`.
- `Governance/Schemas/AXI-SCH-007_Platform_Object.json` publishes the
  `AXI-CLI` namespace and `CLI` object type.
- `Runtime/CLI/` does not yet exist.

---

# Placeholder Rule

No command vocabulary, command syntax, flag set, serialization format,
or exit-code contract is approved by this placeholder.

Implementation may not begin until approved contract content replaces
this placeholder.
